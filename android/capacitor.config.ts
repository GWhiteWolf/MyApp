import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.marcapasos.app',
  appName: 'Marcapasos',
  webDir: 'http://localhost:8100/login'
};

export default config;
